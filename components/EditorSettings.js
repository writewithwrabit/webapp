import SectionPanel from '../components/SectionPanel';

const EditorSettings = () => (
  <div className="w-4/5">
    <SectionPanel>
      Customizable editor coming soon!
    </SectionPanel>
  </div>
);

export default EditorSettings;
